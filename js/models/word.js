define([
    'underscore',
    'backbone'
], function(_, Backbone) {

    var wordModel = Backbone.Model.extend({

        defaults: {
        },

        initialize: function(){
        }
    });

    return wordModel;
});
