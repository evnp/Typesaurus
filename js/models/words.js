define([
    'underscore',
    'backbone'
], function(_, Backbone) {

    var wordsModel = Backbone.Model.extend({

        defaults: {
        },

        initialize: function(){
        }
    });

    return wordsModel;
});
