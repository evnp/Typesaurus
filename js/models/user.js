define([
    'underscore',
    'backbone'
], function(_, Backbone) {

    var userModel = Backbone.Model.extend({

        defaults: {
        },

        initialize: function(){
        }
    });

    return userModel;
});
