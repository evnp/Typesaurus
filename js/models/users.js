define([
    'underscore',
    'backbone'
], function(_, Backbone) {

    var usersModel = Backbone.Model.extend({

        defaults: {
        },

        initialize: function(){
        }
    });

    return usersModel;
});
